.welcome {
  background-image: url("<%= asset_path('welcome.jpeg') %>");
  width: 100%;
  background-size: cover;
  color: #fff;
  min-height: 100vh;
  background-position: center;
  display: flex;
  align-items: center;
  padding: 15px;
}