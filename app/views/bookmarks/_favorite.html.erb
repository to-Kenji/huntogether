<%= form_with(model: current_user.bookmarks.build, remote: true) do |f| %>
  <%= hidden_field_tag :technique_id, @technique.id %>
  <%= f.button :type => "submit", class: 'favorite-icon btn btn-lg btn-light' do %>
    <i class="far fa-bookmark"></i>
  <% end %>
<% end %>
