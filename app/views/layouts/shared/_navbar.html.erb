<header class="mb-4">
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
    <% if user_signed_in? %>
      <%= link_to 'HunTogether', techniques_path, class: 'navbar-brand' %>
    <% else %>
      <a class="navbar-brand" href="/">HunTogether</a>
    <% end %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
        <span class="navbar-toggler-icon"></span>
      </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to techniques_path, class: 'nav-link' do %>
              ハンター養成所 <i class="fas fa-school"></i>
            <% end %>
          </li>
          <li class="nav-item dropdown">
            <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
              救難信号 <i class="fas fa-hands-helping"></i>
            </a>
            <div class="dropdown-menu">
              <a class="dropdown-item" href="#">信号を出す(募集)</a>
              <div class="dropdown-divider"></div>
              <a class="dropdown-item" href="#">信号に参加(探す)</a>
            </div>
          </li>
          <li class="nav-item dropdown">
            <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
              <i class="fas fa-user-circle"></i>
            </a>
            <div class="dropdown-menu">
              <%= link_to user_path(current_user), class: 'dropdown-item'  do %>
                <% if current_user.image? %>
                  <%= image_tag current_user.image.thumb40.url, class: 'icon' %>
                <% else %>
                  <%= image_tag "/assets/default.png", class: 'default-icon-sm' %>
                <% end %>
                マイページ
              <% end %>
              <div class="dropdown-divider"></div>
              <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'dropdown-item'%>
            </div>
          </li>
          
        <% else %>
          <li class="nav-item"><%= link_to 'ユーザー登録', new_user_registration_path, class: 'nav-link' %></li>
          <li class="nav-item"><%= link_to 'ログイン', new_user_session_path, class: 'nav-link' %></li>
        <% end %>
      </ul>
    </div>
  </nav>
</header>